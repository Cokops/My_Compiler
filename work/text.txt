int main() {
    int x = 10;
    int y = 20;
    int z = 5;
    bool cond = (x > 0) && (y < 30) || (z == 5);
    if (cond) {
        return x + y;
    } else {
        return z;
    }
}
// Ожидаемый результат: 30